<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.users_id %> - <%= r.order_id %> - <%= r.price %> - <%= r.date %> - <%= r.complete %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
